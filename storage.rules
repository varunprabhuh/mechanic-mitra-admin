rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read and write access only to authenticated users.
      // This is a secure default for an admin-only application.
      allow read, write: if request.auth != null;
    }
  }
}
